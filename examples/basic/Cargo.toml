[package]
name = "gpui-devtools-basic"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
gpui = { package = "gpui-pre", version = "0.3.5", features = ["inspector"] }
gpui_platform = { package = "gpui-pre-platform", version = "0.3.5", features = ["font-kit", "x11", "wayland", "runtime_shaders"] }
gpui-devtools = { path = "../.." }
